{% extends 'base.html.twig' %}
{% block body %}
    <div class="jumbotron-img jumbotron jumbotron-fluid">
        <div class="container">
            <h1 class="display-4">Artestilo</h1>
            <p class="lead">Artestilo albuns</p>
        </div>
    </div>
    <div class="container">
        <div class="row mb-3">
            <div class="col">
                <button class="btn btn-question">Novos Albuns</button>
            </div>
        </div>

        <div class="row">
            {% for postagem in postagens %}
            <div class="col-4">
                <div id="{{postagem.id}}" class="shadow p-3 mb-5 bg-white rounded" style="box-shadow: 2px 3px 9px 4px rgba(0,0,0,0.04);" >
                    <div class="q-container p-4">

                        <div class="col">
                        <div class="row">
                        <a class="q-title" href="{{ path('album.view',  { id: postagem.id }) }}"><h2>{{ postagem.name }}</h2></a>

                        </div>
                            <div class="row text-center" >
                             
                                   <div class="card-2">
                                <img src="{{ '/uploads/3d9367a31e95e6477d9bca46ba0e7191.png' }}" width="205" height="200">
                                </div>
                            </div>
                          
                        </div>
                    </div>
                    <a class="answer-link" href="#" style="color: #fff;">
                        <p class="q-display-response text-center p-3">
                        
                        </p>
                    </a>
                </div>
            </div>
           
            {% endfor %}
        </div>
    </div>
    <p style="padding-right=100px;">

</p>
{% endblock %}
{% block javascripts %}

<script>
let ids = [];

function getId(id){
     if(ids.find(element=> element == id)){
    console.log("AAAAAAAAAAAAAAAA")
     document.getElementById(id).style.border = "none";
     var index = ids.indexOf(id);
    if (index !== -1) {
     ids.splice(index, 1);
} 
     }else{
           ids.push(id);
     document.getElementById(id).style.border = "thick solid #3d3d40"; 
     document.getElementById(id).style.borderRadius="25px" 
    console.log(ids)
     }
    if(ids.length > 0){
   document.getElementById('togglee').style.visibility = 'visible';
}else{
    document.getElementById('togglee').style.visibility = 'hidden';
}
}
 

</script>
{% endblock %}
